minio:
  enabled: false

gateway:
  enabled: true
  enabledNonEnterprise: true

nginx:
  enabled: false
mimir:
  enterprise:
    enabled: false
  structuredConfig:
    common:
      storage:
        backend: filesystem
    blocks_storage:
      backend: filesystem
      filesystem:
        dir: "/blocks"
    compactor:
      data_dir: "/compactor"
    ruler_storage:
      backend: filesystem
      filesystem:
        dir: "/ruler"
    alertmanager_storage:
      backend: filesystem
      filesystem:
        dir: "/alertmanager"
